//Q6: Print n Positive numbers
let numasInput = parseInt(prompt("Enter a number"));
for (let i = 1; i <= numasInput; i++);
if (i > 0) {
  document.write(i + "its a postive number");
} else {
  document.write(i + " its a negative number");
}
